<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Font color data for the "Add Term" button. -->
    <!-- Pressed state... -->
    <item android:state_pressed="true" android:color="@color/textPressed"/>

    <!-- Focused state... -->
    <item android:state_focused="true" android:color="@color/textFocused"/>

    <!-- Default state. -->
    <item android:color="@color/textGray"/>
</selector>